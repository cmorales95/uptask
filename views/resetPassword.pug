<!DOCTYPE html>
html(lang="es")
    include partials/header
    body.login
        .contenedor-formulario
            h1= nombrePagina
            p Ingresa tu Nuevo Contraseña

            form.caja-login(method="post")
                .campo
                    label(for="password") Nueva Contraseño:
                    input(type="password", name="password" placeholder="Tu password" value= Contraseña)
                .campo.enviar
                    input.boton(type="submit", value="Reestablecer Contraseña")
                